// Render travel destination collection
function tDRender(collection) {    
    const body = document.querySelector("body");
    body.appendChild(collection);
}

// Render delete travel destination collection
function tDDeleteRender(deleteCollection) {    
    const body = document.querySelector("body");
    body.appendChild(deleteCollection);
}